# Fina-Auto Cliente

App para clientes: mapa, pedidos, chat (MVP em construção).

- **Package Android:** `com.finaauto.cliente`
- **Tipo Firestore:** `users.tipo = cliente`

```bash
flutter pub get
dart run flutterfire_cli:flutterfire configure
flutter run
```
